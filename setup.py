from setuptools import setup

setup(name='pyblame',
      version='0.1',
      description='Blame your errors on someone else',
      url='',
      author='Max van Gent',
      author_email='',
      license='MIT',
      packages=['pyblame'],
      zip_safe=True)